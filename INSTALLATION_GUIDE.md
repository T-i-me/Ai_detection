# AI Detection Application  
## Installation & Usage Guide (Python 3.10.11)

This guide provides **step-by-step instructions** to install and run the AI Detection Application using **Python 3.10.11**, which is the recommended and fully compatible version for all dependencies.

---

## 📋 Prerequisites

Before you begin, ensure you have the following installed:

- **Python 3.10.11 (Required)**
- pip ≥ 23.x
- Git (optional)
- (Optional) NVIDIA GPU with CUDA 11.8 for faster inference

> ⚠️ Python 3.11+ is **not recommended** due to limited support for `dlib`, `face-recognition`, and some audio libraries.

---

## 🚀 Installation Steps

### 1️⃣ Navigate to the Project Directory

```bash
cd ai-detection-app
